package main

dyn val : utf8 = "default"

main : proc() = {
    async someFunc("")
    @print(val) // default
}

someFunc : proc(str : utf8) = {
    @print(val) // default
    val = "overwrite"
    @print(val) // overwrite
}