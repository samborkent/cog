package main

dyn val : utf8 = "default"
dyn other : uint64 // valid, will have zero value as default

main : proc() = {
    //someFunc("")
    @print(val) // default
}

/*
someFunc : proc(str : utf8) = {
    @print(val) // default
    val = "overwrite"
    @print(val) // overwrite
}
*/
